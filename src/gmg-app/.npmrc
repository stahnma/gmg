# react-s-alert@1 and react-chartjs-2@2 still declare React 16 peer deps.
# They are replaced/upgraded in Phase 3; until then allow the install to
# proceed under React 18 instead of failing ERESOLVE.
legacy-peer-deps=true
